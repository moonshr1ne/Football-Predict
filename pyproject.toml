[project]
name = "barcelona-match-lab"
version = "0.1.0"
description = "Leak-free local predictor for FC Barcelona matches in La Liga and the UEFA Champions League."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["scikit-learn>=1.6,<2"]

[project.scripts]
nfp = "football_predictor.cli:main"
barca-lab = "football_predictor.cli:main"
